import "./globals.css";

export const metadata = {
  title: "Hasan El-haj Portfolio",
  description: "Portfolio CLI",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
       
      >
        {children}
      </body>
    </html>
  );
}
